# Production Environment Variables Template
# Copy this to .env and update values for your production deployment

# Environment
ENV=production

# Application
APP_NAME=Your NGO Name - Nonprofit Intelligence Platform
APP_URL=https://your-domain.org

# Database Options (choose one):

# Option 1: Continue using SQLite (easiest, good for <1M records)
# No configuration needed, database will be created automatically

# Option 2: PostgreSQL (for larger scale)
# Free options:
# - Supabase: 500MB free - https://supabase.com
# - Neon: 3GB free - https://neon.tech
# - Render: Free tier available - https://render.com
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 3: MySQL (if your hosting provides it)
# DATABASE_URL=mysql://user:password@host:port/database

# CORS Origins (your domain)
CORS_ORIGINS=https://your-domain.org,https://www.your-domain.org

# Performance
BATCH_SIZE=1000
MAX_WORKERS=4

# Cache TTL (seconds)
CACHE_TTL=3600

# Security (IMPORTANT: Generate a strong secret key!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-production-secret-key-here

# Optional: Email notifications (if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Optional: Analytics
# GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X
